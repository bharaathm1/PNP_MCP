# MCP Stdio Servers — minimal requirements
# Only the packages actually imported by the two stdio servers.
# Run setup.bat to create venv and install.

# MCP framework
fastmcp>=2.13.0,<3.0
mcp>=1.22.0

# Config / env
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# Data analysis
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0

# Knowledge base storage (pure-Python, no MongoDB server needed)
mongita>=1.2.0

# HTTP / async (fastmcp transitive deps + speedlibs_service_client)
requests>=2.28.0
httpx>=0.27.0
anyio>=4.0.0
starlette>=0.40.0
uvicorn>=0.30.0
